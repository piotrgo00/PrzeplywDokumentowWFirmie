
@{
    ViewBag.Title = "GetInvoiceButton";
    string orderId = this.ViewData["id"].ToString();
    bool isFinished = (bool)this.ViewData["isFinished"];
}

@if (isFinished)
{
    @Html.ActionLink("Get Invoice", "Invoice", new { id = orderId })
}
