@{
    ViewBag.Title = "EditButton";
    string orderId = this.ViewData["id"].ToString();
    bool isEditable = (bool)this.ViewData["isEditable"];
}

@if (isEditable) 
{
    @Html.ActionLink("Complete order", "Edit", new { id = orderId }) 
}