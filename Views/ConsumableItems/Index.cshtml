@model IEnumerable<PrzeplywDokumentowWFirmie.Models.ConsumableItem>

@{
    ViewBag.Title = "Index";
}

<h2>Consumable items</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ExpirationDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Price)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ExpirationDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Price)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ConsumableItemId }) |
            @*@Html.ActionLink("Details", "Details", new { id=item.ConsumableItemId }) |*@
            @Html.ActionLink("Delete", "Delete", new { id=item.ConsumableItemId })
        </td>
    </tr>
}

</table>
